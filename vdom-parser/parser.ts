import { Vdom } from "../vdom-struct/type.ts";
import { Element } from "../vdom-struct/type.ts";

export function Parser(vdom: Vdom): Element {

    let result = "";

    for (let i = 0; i < vdom.children.length; i++) {
        
    }

    return {};
}